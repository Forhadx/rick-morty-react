import React from "react";
import "./Pagination.scss";

const Pagination = () => {
  return (
    <div className="paginations">
      <button>PREV</button>
      <button>1</button>
      <button>NEXT</button>
    </div>
  );
};

export default Pagination;
